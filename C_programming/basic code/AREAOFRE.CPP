#include<stdio.h>
#include<conio.h>

void main(){
clrscr();
int length,height,area;
  printf("Enter the length and height of rectangle :");
  scanf("%d%d",&length,&height);

  area=length*height;

  printf("the area rectangle is %d",area);
  getch();
  }


